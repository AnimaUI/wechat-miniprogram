// 获取应用实例
const app = getApp();
let {
    colour
} = require('../../../utils/colour');
Page({
    data: {
        imgBaseUrl: app.imgBaseUrl,
        colour
    },
    onLoad() {},
    onShareAppMessage() {},
    onShow() {}
});
